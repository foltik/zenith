[package]
name = "substrate"
authors.workspace = true
version.workspace = true
edition.workspace = true

[lib]
path = "lib.rs"

[dependencies]
zenith = { path = "../zenith", features = ["bin"] }
stud = { workspace = true, features = ["error", "log"] }
